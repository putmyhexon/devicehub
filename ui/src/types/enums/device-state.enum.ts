export enum DeviceState {
  ABSENT = 'absent',
  OFFLINE = 'offline',
  UNAUTHORIZED = 'unauthorized',
  PREPARING = 'preparing',
  BUSY = 'busy',
  AVAILABLE = 'available',
  USING = 'using',
  AUTOMATION = 'automation',
  PRESENT = 'present',
  UNHEALTHY = 'unhealthy',
}
